<!--
  THE RUBRIC LOOP — the nine dimensions drawn as one cycle.
  R1–R4 are the learning half (golden set → eval → gate → diagnose);
  R5–R9 are the operating half (what happens once a version is live).
  Same contract as _templates/agent-structure.svg: class="dia", no width/height,
  role="img" + aria-label, <title> in every <g>, unique marker id, styling in main.css.
-->
<svg class="dia" viewBox="0 0 360 170" role="img"
     aria-label="The loop the nine rubric dimensions describe: a versioned golden set feeds an eval run graded by deterministic graders and an independent judge; the eval run feeds a regression gate; a version the gate passes ships to production, where guardrails, traces, cost caps and the scale signal apply; production traces feed a diagnosis that proposes the next version back into the eval run, and a version the gate refuses goes to diagnosis instead of to users.">
  <defs>
    <marker id="ar-loop" viewBox="0 0 8 8" refX="7" refY="4" markerWidth="7" markerHeight="7" orient="auto">
      <path d="M0,0 L8,4 L0,8 z" fill="currentColor"/>
    </marker>
  </defs>

  <text class="cap" x="6" y="12">R1–R4 · the learning half  ·</text>
  <text class="cap" x="6" y="25">R5–R9 · the operating half</text>

  <g><title>R1 — a versioned golden set: fixed cases, committed predictions, reproducible offline</title>
    <rect class="nd" x="6" y="32" width="94" height="44" rx="8"/>
    <text class="tx" x="53" y="51">golden set</text>
    <text class="tx s" x="53" y="66">R1 · fixed</text></g>

  <g><title>The eval run — deterministic graders plus an LLM judge that is independent of the model being graded (R1, R2)</title>
    <rect class="nd" x="116" y="32" width="112" height="44" rx="8"/>
    <text class="tx" x="172" y="51">eval + judge</text>
    <text class="tx s" x="172" y="66">R1 · R2</text></g>

  <g><title>R3 — the regression gate: an eval floor and no per-case pass to fail flips</title>
    <rect class="nd hi" x="244" y="32" width="48" height="44" rx="8"/>
    <text class="tx" x="268" y="51">gate</text>
    <text class="tx s" x="268" y="66">R3</text></g>

  <g><title>R8 — merge is the deploy: keyless OIDC CI/CD with a smoke test and a rollback path</title>
    <rect class="nd" x="306" y="32" width="48" height="44" rx="8"/>
    <text class="tx" x="330" y="51">ship</text>
    <text class="tx s" x="330" y="66">R8</text></g>

  <g><title>R4 — the learning loop: diagnose the traces, propose one lever, verify it against the golden set before it can ship</title>
    <rect class="nd hi" x="110" y="92" width="110" height="46" rx="8"/>
    <text class="tx" x="165" y="111">diagnose</text>
    <text class="tx s" x="165" y="126">R4 · one lever</text></g>

  <g><title>Production — guardrails around every action (R5), a trace per request (R6), cost per answer and its caps (R7), and the saturation signal that says when to move up the ladder (R9)</title>
    <rect class="nd hi" x="254" y="92" width="100" height="46" rx="8"/>
    <text class="tx" x="304" y="106">production</text>
    <text class="tx s" x="304" y="120">R5 · R6</text>
    <text class="tx s" x="304" y="134">R7 · R9</text></g>

  <path class="ed" d="M100,54 H114" marker-end="url(#ar-loop)"/>
  <path class="ed" d="M228,54 H242" marker-end="url(#ar-loop)"/>
  <path class="ed" d="M292,54 H304" marker-end="url(#ar-loop)"/>
  <path class="ed" d="M330,76 V80 Q330,86 324,86 H310 Q304,86 304,92" marker-end="url(#ar-loop)">
    <title>a version the gate passes is what reaches users</title></path>
  <path class="ed" d="M254,115 H222" marker-end="url(#ar-loop)">
    <title>traces and the judged online sample come back from production</title></path>
  <path class="ed" d="M165,92 V78" marker-end="url(#ar-loop)">
    <title>the proposed next version is re-evaluated before it can ship</title></path>
  <path class="ed dash" d="M268,76 V80 Q268,86 262,86 H212 Q206,86 206,92" marker-end="url(#ar-loop)">
    <title>a version the gate refuses goes back to diagnosis, not to users</title></path>

  <text class="cap" x="6" y="150">a version the gate refuses goes back to</text>
  <text class="cap" x="6" y="163">diagnose, never to users</text>
</svg>
