<svg class="dia" viewBox="0 0 360 160" role="img"
     aria-label="ConvFinQA agent structure: a turn — the question plus the filing and the conversation history — passes through four typed pydantic-ai agents in sequence, triage then preprocess then retriever then calculator, all four loading one versioned prompt set; number turns short-circuit at the retriever and go straight to the answer, while program turns are computed through six explicit calculator tools.">
  <defs>
    <marker id="ar-convfinqa" viewBox="0 0 8 8" refX="7" refY="4" markerWidth="7" markerHeight="7" orient="auto">
      <path d="M0,0 L8,4 L0,8 z" fill="currentColor"/>
    </marker>
  </defs>

  <g><title>the turn — question plus the filing (text and table) plus the conversation history; roughly 60% of questions refer back to an earlier turn</title>
    <rect class="nd" x="6" y="12" width="70" height="44" rx="8"/>
    <text class="tx" x="41" y="30">turn</text>
    <text class="tx s" x="41" y="45">+ report</text></g>

  <g><title>triage agent — classifies the turn as number or program, and the conversation as Type I or Type II</title>
    <rect class="nd" x="90" y="12" width="76" height="44" rx="8"/>
    <text class="tx" x="128" y="30">triage</text>
    <text class="tx s" x="128" y="45">turn type</text></g>

  <g><title>preprocess agent — resolves "that" and "this change" against history, then decomposes the question into retrieval sub-questions and a calculation program</title>
    <rect class="nd hi" x="180" y="12" width="94" height="44" rx="8"/>
    <text class="tx" x="227" y="30">preprocess</text>
    <text class="tx s" x="227" y="45">program</text></g>

  <g><title>the versioned prompt set — v1, v2, generated v3_1; a module per version, auto-discovered, loaded by all four agents</title>
    <rect class="nd" x="288" y="12" width="64" height="44" rx="8"/>
    <text class="tx" x="320" y="30">prompt</text>
    <text class="tx s" x="320" y="45">set v2</text></g>

  <g><title>retriever agent — pulls the raw values out of the report's text and table, and answers number turns directly</title>
    <rect class="nd hi" x="6" y="80" width="88" height="44" rx="8"/>
    <text class="tx" x="50" y="98">retriever</text>
    <text class="tx s" x="50" y="113">values</text></g>

  <g><title>calculator agent — executes the program through six explicit tools (add, subtract, multiply, divide, exp, greater), never free-text arithmetic</title>
    <rect class="nd hi" x="110" y="80" width="94" height="44" rx="8"/>
    <text class="tx" x="157" y="98">calculator</text>
    <text class="tx s" x="157" y="113">6 tools</text></g>

  <g><title>the answer — a number, scored against gold by exact match and by program match; the whole turn is written to the trace store</title>
    <rect class="nd" x="220" y="80" width="64" height="44" rx="8"/>
    <text class="tx" x="252" y="106">answer</text></g>

  <path class="ed" d="M76,34 H88" marker-end="url(#ar-convfinqa)"/>
  <path class="ed" d="M166,34 H178" marker-end="url(#ar-convfinqa)"/>
  <path class="ed" d="M212,56 V58 Q212,64 206,64 H56 Q50,64 50,70 V78" marker-end="url(#ar-convfinqa)">
    <title>the decomposed program and sub-questions go to the retriever</title></path>
  <path class="ed" d="M94,102 H108" marker-end="url(#ar-convfinqa)"/>
  <path class="ed" d="M204,102 H218" marker-end="url(#ar-convfinqa)"/>
  <path class="ed dash" d="M70,80 V76 Q70,72 74,72 H248 Q252,72 252,76 V78" marker-end="url(#ar-convfinqa)">
    <title>number turns short-circuit at the retriever and never reach the calculator</title></path>
  <path class="ed dash" d="M320,56 V58 Q320,64 314,64 H250 Q244,64 244,58" marker-end="url(#ar-convfinqa)">
    <title>one versioned prompt set is loaded by all four agents</title></path>

  <text class="cap" x="6" y="140">champion v2 · v3_1 refused by the gate</text>
  <text class="cap" x="6" y="154">every turn traced</text>
</svg>
